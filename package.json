{
  "private": true,
  "type": "module",
  "version": "0.0.0",
  "description": "Workspace root for fncostguard",
  "scripts": {
    "build": "pnpm --filter fncostguard build",
    "test": "pnpm --filter fncostguard-tests test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.6.0"
  },
  "packageManager": "pnpm@10.17.0",
  "engines": {
    "node": ">=20"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
